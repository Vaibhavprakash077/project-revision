name: Python-CI

on:
  push:
    branches:
      - main
    paths:
      - "**.py"
  pull_request:
    branches:
      - main
  
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: checkout repository
        uses: actions/checkout@v4
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Python version
        run: python --version
  security:
    runs-on: ubuntu-latest
    steps:
      - name: cat docs
        run: cat docs.txt
  build:
    runs-on: ubuntu-latest
    needs: [lint,security,test]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      
  
        